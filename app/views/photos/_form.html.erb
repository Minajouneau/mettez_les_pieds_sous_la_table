<% restaurant.photos.where(category: category).each do |photo| %>
  <% if photo.file.attached? %>
    <div class="d-inline-flex">
      <div class="position-relative">
        <%= cl_image_tag photo.file.key, crop: :fill, width: 100, height: 100, class: "photo-added" %>
        <div class="photo-trash">
          <%= link_to restaurant_photo_path(restaurant, photo), method: :delete, class: "remove-photo", remote: true do %>
            <i class="fas fa-trash-alt"></i>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
<%= simple_form_for [restaurant, photo], html: { id: "submit", class: "auto-submit-form" }, remote: true do |f| %>
  <%= f.input :category, input_html: {value: category}, as: :hidden %>
  <div class="add-photo d-flex">
    <%= f.input :file, as: :file, input_html: { class: 'photo-input'}, label_html: { class: 'upload-photo'}, label: 'Télécharger une photo 📷'%>
  </div>
<% end %>